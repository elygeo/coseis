CC = {build_cc}
LIBS = {build_libs}
CFLAGS = {build_cflags} \
    -DNNODE={nnode} \
    -DNZ={nz} \
    -DDELTA={delta} \
    -DZSTART={zstart}

mesh.x : mesh.c Makefile
	$(CC) $(CFLAGS) -o $@ $< $(LIBS)

clean :
	rm -rf run mesh.x mesh.x.dSYM


#!/usr/bin/env python
#!/usr/bin/env ipython --gui=wx -i --
import sys, getopt
import cst
opts, faults = getopt.getopt(sys.argv[1:], 's')
opts = dict(opts)
split = '-s' in opts
cst.cfm.explore(faults, split)


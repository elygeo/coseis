#!/usr/bin/env python
#!/usr/bin/env ipython --gui=wx -i --
import sys, getopt
import cst
opts, faults = getopt.getopt(sys.argv[1:], 's:')
opts = dict(opts)
if faults == []:
    faults = '*'
if '-s' in opts:
    cst.cfm.explore(faults, int(opts['-s']))
else:
    cst.cfm.explore(faults)


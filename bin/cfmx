#!/usr/bin/env python
#!/usr/bin/env ipython --gui=wx -i --
import sys, getopt
import cst
opts, faults = getopt.getopt(sys.argv[1:], 'sn')
opts = dict(opts)
split = '-s' in opts
basemap = '-n' not in opts
cst.cfm.explore(faults, split, basemap)


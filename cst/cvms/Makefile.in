FC = {build_fc}
FFLAGS = {build_fflags}
LIBS = {build_libs}

cvms.x : io{mode}.o version{version}.o
	$(FC) $(FFLAGS) -o $@ $^ $(LIBS)

clean :
	rm -f *.o *.x *.lst *.pyc

distclean : clean
	rm Makefile


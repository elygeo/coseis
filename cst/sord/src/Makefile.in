flags = {build_flags} {build_debug}
flags = {build_flags} {build_real8}
flags = {build_flags} {build_prof}
flags = {build_flags}
cc = {build_cc} $(flags) -c
fc = {build_fc} $(flags) -c
ld = {build_ld} $(flags) -o
libs = {build_libs}
objects = {objects}

{image}: $(objects)
	$(ld) $@ $^ $(libs)

clean:
	rm -f *.o *.mod *.x *.lst

